generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Purchase {
  id        String   @id @default(cuid())
  userId    String
  memeId    String
  createdAt DateTime @default(now())
}
