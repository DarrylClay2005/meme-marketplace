import { describe, it, expect } from 'vitest';

// This is a placeholder test to demonstrate frontend testing setup.
// You can expand this to render <App /> with @testing-library/react later.

describe('frontend smoke test', () => {
  it('runs test framework', () => {
    expect(1 + 1).toBe(2);
  });
});
